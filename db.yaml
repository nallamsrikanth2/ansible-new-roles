- name: congigure the db server
  hosts: db
  become: yes
  vars_files:
    credentials.yaml
  vars_prompt:
    - name: password
      prompt: enter your password?
      private: false
  roles:
  - db
